# URL of the callcenter-qa-api backend. Must be reachable from the browser
# (this is a client-visible variable - NEXT_PUBLIC_ prefix required by Next.js).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
